[tool.poetry]
name = "chart"
version = "0.1.0"
description = "A Fixie agent for creating charts."
authors = ["Fixie <hello@fixie.ai>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.9"
fixieai = "^0.1.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
